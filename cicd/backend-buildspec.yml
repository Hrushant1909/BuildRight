version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  pre_build:
    commands:
      - cd backend
  build:
    commands:
      - mvn clean package

artifacts:
  base-directory: backend/target
  files:
    - '*.jar'
